# MySQL connection string
# Format: user:password@tcp(host:port)/dbname?parseTime=true
DB_DSN=root:password@tcp(localhost:3306)/grc_platform?parseTime=true

# HTTP listen port (default: 8080)
SERVER_PORT=8080
